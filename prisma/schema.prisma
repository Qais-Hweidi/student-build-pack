generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl  = env("DIRECT_URL")
}

// Models will be added in the next task (Database Schema Design)
// This schema is ready for the service provider and analytics models
